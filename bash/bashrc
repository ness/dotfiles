#############
# ssh 
function ssh_add_conditional {
	ssh-add -l > /dev/null
	if [ ! $? ]; then
		ssh-add;
	fi
}

alias ssh="ssh_add_conditional; ssh"


############
# virtualenv
export WORKON_HOME=~/code/python/virtualenv
source ~/.local/bin/virtualenvwrapper.sh

############
# git
alias gits='git status'
alias gitd='git diff'
alias gita='git add'
alias gitau='git add -u'
alias gitc='git commit'

############
# xrandr
alias vgaoff='xrandr --output VGA1 --off'
alias vgaon='xrandr --output VGA1 --left-of LVDS1 --auto'

############
# various 
PATH=~/bin:$PATH
PS1='[\u@\h \W]\$ '
alias ls='ls --color=auto'
alias ydl='youtube-dl -f 18 --title'
alias ydlm='youtube-dl -f 18 --title --extract-audio --audio-format vorbis'
